import { compose, toArray } from './module/function.modules';

import { each, getType, isType, mixin } from './module/object.modules';

export {
  compose,
  toArray,
  each,
  getType,
  isType,
  mixin
};
